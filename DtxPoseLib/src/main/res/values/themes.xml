<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="FullScreenTranslucent" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/transparent</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="common_dialog_style_game" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- Dialog的windowFrame框为无 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否漂现在activity上 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 是否半透明 -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:background">@null</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 去除黑色边框的关键设置项 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 屏幕背景是否变暗 -->
        <item name="android:backgroundDimAmount">0.5</item>
        <!--<item name="android:activityOpenEnterAnimation">@style/dialog_anim</item>-->
        <item name="android:windowAnimationStyle">@style/dialog_anim_game</item>
    </style>

    <style name="dialog_anim_game">
        <item name="android:windowEnterAnimation">@anim/dialog_in_anim_game</item>
        <item name="android:windowExitAnimation">@anim/dialog_out_anim_game</item>
    </style>
</resources>